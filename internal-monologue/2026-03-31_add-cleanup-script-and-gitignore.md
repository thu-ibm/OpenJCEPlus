# Cleanup Script and .gitignore Update

Date: 2026-03-31
Action: Created `cleanup.bat` to automate removal of build intermediates, dumps, trace logs, and test artifacts, and added `cleanup.bat` to `.gitignore`.
