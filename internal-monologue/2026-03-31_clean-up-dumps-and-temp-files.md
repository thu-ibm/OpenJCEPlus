# Cleanup of Diagnostic and Dump Files

Date: 2026-03-31
Action: Removed JVM crash dumps (`core.*.dmp`), snapshot traces (`Snap.*.trc`), javacores (`javacore.*.txt`), jitdumps (`jitdump.*.dmp`), temporary adapter buffers (`.bob_tmp_*.txt`), and test output logs (`suite_out*.txt`, `suite_baseline.txt`, `build_openssl.txt`).
